# Used only by scripts/configure-wasender.mjs — NOT loaded on Firebase deploy.
# Copy to functions/.env.wasender and run: node scripts/configure-wasender.mjs

WASENDER_ACCESS_TOKEN=your_wasender_api_bearer_token
WASENDER_INSTANCE_ID=your_whatsapp_session_instance_id
